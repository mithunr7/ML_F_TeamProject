Running the code:
- Save the main.py file
- Run the gradio.py file.

We have locally saved the dataset and accessed the local directory but in the ui you can drop json files which has the required attributes and then run the training model and also use the interactive portal to check if a review is classified popular or not.